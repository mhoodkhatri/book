# Core dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# HTTP client for remote embedding API
httpx>=0.27.0

# Groq LLM (primary chat provider)
groq>=0.4.0

# Vector database
qdrant-client>=1.7.0

# Data validation
pydantic>=2.0.0

# SSE streaming
sse-starlette>=2.0.0

# HTML parsing (translation feature)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Auth session validation (shared Neon Postgres)
asyncpg>=0.29.0
